import Calculable from "./Numbers/Calculable";

/**
 * 
 * @param {Calculable} a 
 * @param {Calculable} b 
 */
export function sum(a, b) {
  if(/** a, b 둘 중 하나라도 Calculable이 아닐 때 */) {
    // Throw Error('다룰 수 없는 대상')
  }
  if(/** a, b 둘 다 Calculable이지만 타입이 서로 다를 때 */) {
    
  }
  if(/** a, b 모두 CalculableNumber */) {
    
  }
  if(/** a, b 모두 CalculableString */) {

  }
}